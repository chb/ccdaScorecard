<% if (misses.length > 0) {%>

<h2>
<%= misses.length %> of <%= (misses.length + hits.length)%> 
codes didn't match their displayName</h2>
<table class="table table-bordered table-striped">
<tr>
<th colspan="2">Your code</th>
<th>Preferred term</th>
<th>What now?</th>
</tr>

<% misses.forEach(function(miss){ 

console.log(miss);
%>
<tr>
<td>
<code><%= miss.codeSystemName %>:<%= miss.code %></code> 
</td>
<td>
"<%= miss.displayName %>"
</td>
<td>
"<%= miss.normalized.conceptName %>"
</td>
<td>
See <a target="_blank" href="<%= miss.normalized._id %>"><%= miss.normalized._id %></a>
</td>
</tr>
<% }) %>
</table>
<% } %>
